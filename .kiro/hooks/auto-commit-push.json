{
  "version": "v1",
  "hooks": [
    {
      "name": "Auto Commit & Push on Save",
      "trigger": "PostFileSave",
      "matcher": "\\.(html|js|json|css)$",
      "action": {
        "type": "command",
        "command": "powershell -NoProfile -Command \"Set-Location 'c:\\Users\\zbaze\\ToiletAdvisor'; git add -A; $diff = git diff --cached --name-only; if ($diff) { git commit -m 'auto: save changes'; git push origin main }\"",
        "timeout": 60
      }
    }
  ]
}
