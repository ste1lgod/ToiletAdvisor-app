{
  "version": "v1",
  "hooks": [
    {
      "name": "Auto Commit & Push on Save",
      "trigger": "PostFileSave",
      "matcher": "\\.(html|js|json|css|bat|ps1|yml|md)$",
      "action": {
        "type": "command",
        "command": "powershell -NoProfile -Command \"$repo = (Get-Location).Path; Set-Location $repo; git add -A; $diff = git diff --cached --name-only; if ($diff) { $ts = Get-Date -Format 'yyyy-MM-dd HH:mm:ss'; git commit -m \\\"auto: save changes ($ts)\\\"; git push origin main }\"",
        "timeout": 60
      }
    }
  ]
}
