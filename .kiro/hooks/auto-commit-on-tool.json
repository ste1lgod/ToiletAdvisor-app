{
  "version": "v1",
  "hooks": [
    {
      "name": "Auto Commit After Agent File Changes",
      "trigger": "PostToolUse",
      "matcher": "^(fs_write|str_replace|fs_append)$",
      "action": {
        "type": "command",
        "command": "powershell -NoProfile -Command \"Set-Location 'c:\\Users\\zbaze\\ToiletAdvisor'; Start-Sleep -Milliseconds 500; git add -A; $diff = git diff --cached --name-only; if ($diff) { git commit -m 'auto: agent file changes'; git push origin main }\"",
        "timeout": 60
      }
    }
  ]
}
